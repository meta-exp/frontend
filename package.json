{
  "name": "metaexp",
  "version": "0.1.0",
  "engines": {
    "yarn": "1.5.1"
  },
  "scripts": {
    "heroku-postbuild": "yarn install --prefix pika --only=production && cd pika && yarn add graph-app-kit --registry https://neo.jfrog.io/neo/api/npm/npm && npm run build"
  },
  "dependencies": {
    "yarn": "^1.5.1"
  }
}
